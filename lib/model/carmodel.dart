class OpposingCar {
  double positionX; // X position (lane)
  double positionY; // Y position (vertical movement)
  double speed;     // Speed of the car

  OpposingCar({required this.positionX, required this.positionY, required this.speed});
}
